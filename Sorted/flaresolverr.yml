services:
  flaresolverr:
    image: ghcr.io/flaresolverr/flaresolverr:latest
    container_name: flaresolverr
    ports:
      - 8191:8191
    volumes:
      - /var/lib/flaresolver:/config
    environment:
      - TZ=Asia/Shanghai
    restart: always
    network_mode: bridge
